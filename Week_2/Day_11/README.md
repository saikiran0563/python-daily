# Day 4 - Polymorphism in Python

## Topics Covered

- Polymorphism
- Method overriding
- Duck typing
- Inheritance-based polymorphism
- Polymorphism with functions
- Built-in polymorphism
- `len()`
- `+` operator

## What is Polymorphism?

Polymorphism means that the same method, function, or operation can behave differently depending on the object or data type.

## Method Overriding and Polymorphism

A child class can override a method of its parent class.

The same method call can then produce different behavior depending on the object.

Example:

```python
animal.sound()